
@model IEnumerable<WebApplication7.Models.Faculty>
@using WebApplication7.Models;


<h2>Факультеты</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dean_FN)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dean_FN)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Title }) |
                @Html.ActionLink("Details", "Details", new { id = item.Title }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Title })
            </td>
        </tr>
    }

</table>
