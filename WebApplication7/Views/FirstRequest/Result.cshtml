@model IEnumerable<WebApplication7.Models.FIRST_Result>

@using WebApplication7.Models;

@{
    ViewBag.Title = "Список указанных студентов";
    Layout = "~/Views/Shared/_Layout.cshtml";
    Pager pager = new Pager();

    int pageNo = 0;
    if (ViewBag.Pager != null)
    {
        pager = ViewBag.Pager;
        pageNo = pager.CurrentPage;
    }
}
<h2 class="mt-4">Первый запрос </h2>
<table class="table table-striped mt-3">
    <tr>
        <th>
            Имя
        </th>
        <th>
            Номер группы
        </th>
        <th>
            Средняя зарплата
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Group_2_Cipher)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.avgSalary)
            </td>
        </tr>
    }
</table>
<div class="container">
    @if (pager.TotalPages > 0)
    {
        <ul class="pagination justify-content-end">
            @for (var pge = pager.StartPage; pge <= pager.EndPage; pge++)
            {
                <li class="page-item @(pge == pager.CurrentPage ? "active" : "")">
                    <a class="page-link" href="?pg=@pge">@pge</a>
                </li>

            }
        </ul>
    }
</div>